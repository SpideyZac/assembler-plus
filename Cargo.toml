[package]
name = "assembler-plus"
version = "0.1.0"
edition = "2021"

[profile.dev.build-override]
opt-level = 3

[dependencies]
laps = { version = "0.1.7", features = ["macros"] }

[build-dependencies]
copy_to_output = "2.2.0"
